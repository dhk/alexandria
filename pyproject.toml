[project]
name = "alexandria"
version = "0.1.0"
description = "Alexandria research corpus — the evidence trail. Tooling now lives in dhk/minority-report."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pyyaml>=6.0",
]

[dependency-groups]
dev = [
  "ruff>=0.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"
